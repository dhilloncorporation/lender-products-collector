# Environment Setup

## Required

Create `.env` file:
```env
OPENAI_API_KEY=your_key
```

## Optional

```env
CUSTOM_SEARCH_API_KEY=your_key
CUSTOM_SEARCH_CX=your_cse_id
OPENAI_MODEL=gpt-4o-mini
```

**Note**: System auto-loads `.env` on startup.
